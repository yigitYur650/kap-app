import 'package:flutter/material.dart';

class KapColors {
  // Pure White Background
  static const Color pureWhite = Color(0xFFFFFFFF);

  // Soft Charcoal/Anthracite Text
  static const Color slateDark = Color(0xFF1C1C1E);

  // Light Slate Border
  static const Color borderLight = Color(0xFFE2E8F0);

  // Warm Cream / Broken White Background (#FAF8F5)
  static const Color backgroundLight = Color(0xFFFAF8F5);

  // Sakinleştirici, Tok Kiremit / Yanık Turuncu (Terracotta)
  static const Color primaryAccent = Color(0xFFC05A22);

  // Yumuşak, kiremit tonlarında pastel kırmızı (Muted Red)
  static const Color mutedRed = Color(0xFFD35C4E);
}
